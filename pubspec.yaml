name: useradgents_analysis
description: Lint and DCM rules for Dart and Flutter used internally at UserAdgents.
version: 1.0.0
repository: https://github.com/useradgents/useradgents_analysis.git

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: "^6.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
